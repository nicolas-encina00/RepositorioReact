import React from "react";
import Home from "../pages/Home";

export const ItemListContainer = ({children}) => {
    return (
        <div>
            <h1>{children}</h1>
        </div>

    )
}